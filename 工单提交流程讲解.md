## 工单提交流程讲解

整个提交流程分**AI 预审与人工转接**、**前置校验**、**后置生成**三段，对应协议书里的 P0 边界。

**一、AI 预审与人工转接（提交前）**

在员工正式填写工单前，先过一道 AI 预审，能自助解决的就不进工单池：

1. **AI 尝试解决**：员工描述问题后，AI 基于知识库与历史工单给出解决方案（如重启、重装、改配置等常见指引）。
2. **解决成功**：员工按指引操作后问题消除，流程结束，**不生成工单**。
3. **AI 无法解决 → 转人工**：AI 判定无法解决，或员工反馈未解决，自动转接人工客服/工程师。
4. **人工了解情况**：人工与员工沟通，确认问题细节、复现条件、影响范围。
5. **人工决定是否生成工单**：人工核实后决定是否放行——确认需派单则进入前置校验流程；判定为重复/无效/已解决则驳回。

> 即工单不是员工想提就能提，而是 **AI 先试 → 不行转人工 → 人工核实后才允许生成**。后续的前置校验与后置生成，都发生在人工放行之后。

**二、填写与前置校验（用户侧）**

员工在工单提交页填写七个字段：

| 字段                 | 校验要点                                                 |
| -------------------- | -------------------------------------------------------- |
| 标题 title           | 必填，长度 ≤ 50                                          |
| 分类 category        | 必填，取值 ∈ {硬件/软件/网络/账号/其他}                  |
| 优先级 priority      | 必填，高/中/低；高优会同步短信通知                       |
| 描述 description     | 必填，10 ~ 500 字                                        |
| 附件 attachment_urls | jpg/png，单张 ≤ 5MB，最多 3 张，**嗅探真实 MIME 防伪造** |
| 期望完成时间         | 选填，不得早于当前时间                                   |
| 资产编号 asset_id    | 选填，正则 `^IT-[A-Z]{2,4}-\d{8}$` + CMDB 联动回显       |

**提交校验**（点提交时统一复核七字段）。任一字段不合规，提交按钮置灰、对应字段飘红提示，**不生成工单**。分类选中后还会经 300ms 防抖触发知识库推荐。

**三、提交与后置生成（系统侧）**

校验全部通过后：

1. **防重**：提交按钮 3s 防抖，期间重复点击只落库一条（幂等）。
2. **落库**：后端将各字段写入 `ticket` 表，生成符合 `TK+14位日期+4位流水` 格式的 ticket_id，状态置为 `CREATED`，记录创建人与时间。
3. **通知**：触发 ASSIGNED 前置通知事件，走站内信告知相关方。
4. **跳转**：前端跳转成功页，端到端延迟 P95 ≤ 1500ms。



